on:
# runs through 1. GitHub GUI  2. GitHub CLI tool
  workflow_dispatch:
    inputs:
      environment:
        description: "Choose environment"
        required: true
        type: choice
        default: "statging"
        options:
          - statging
          - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "Deploy application"
        run: echo "Deploying application to ${{ inputs.environment }}"
        
